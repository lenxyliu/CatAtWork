# BC-029 — Current frames drift in local character identity

- Status: open
- Classification: current-confirmed
- ISSUE: ISSUE-026; GitHub #21
- First observed: 2026-07-24
- Affected versions: asset 2026.07.23.6 at commit `0ca2b518d5a079ace1ef1e6d9a6892a5259b080e`
- Fixed versions: none
- Regression test: B1 proxy scan and human contact-sheet review; canonical identity rig not defined
- Automated evidence: QA root `Assets/CatAtWork/qa/b1-current-build-0ca2b518`; scope digest `f687b3b19a3caa1453abd9e5b35a48779c721a0bbe7a069f63b318ea4e598ada`
- Device evidence: `happy`, `failed`, `runRight` and `bellyRoll` current sheets inspected

## Preconditions

Root-align current frames and compare same-pose/same-view identity proxies:
upper silhouette/head width, eye-mask support, face-mask shape, limb/tail
topology and local texture. Keep overall scale and color as separately
reported covariates.

## Reproduction

1. Generate the all-action sheet plus the four scoped action sheets.
2. Review frame continuity at native crop resolution.
3. Compare current endpoint identity proxies in `endpoint-matrix.json`.
4. Do not treat global rescaling as an identity repair.

## Expected

Comparable views stay within the Issue's 3–5% local anatomy budgets and retain
the same face, mask, ears, limbs and tail topology.

## Actual

Comparable seated starts vary from upper-silhouette width **119 px**
(`happy`/`wave`) to **172–174 px** (`curious`/`petting`); eye-mask support
varies from 233 (`wave`) to 512 (`curious`). Human review confirms the
previously scoped face/mask, tail and limb discontinuities in `happy`,
`failed`, `runRight` and low-pose actions after root alignment.

## Root cause

Confirmed failure scope: local anatomy and identity cues are inconsistent in
the current atlas content. A canonical identity rig and causal provenance are
not established in B1.

## Impact

The character can look like a different cat even after global scale and color
normalization.

## Status timeline

- 2026-07-24: current proxy scan plus human review completed; classified
  `current-confirmed`.
