# BC-023 — Current validator returns green with confirmed visual failures

- Status: open
- Classification: current-confirmed
- ISSUE: ISSUE-020; GitHub #14
- First observed: 2026-07-24
- Affected versions: validator at commit `0ca2b518d5a079ace1ef1e6d9a6892a5259b080e`, asset 2026.07.23.6
- Fixed versions: none
- Regression test: current validator invocation itself is the badcase
- Automated evidence: QA root `Assets/CatAtWork/qa/b1-current-build-0ca2b518`; scope digest `213e079b5b2c0d62f2972e09459313e35c80ae4634178370d4d37554f93f79f1`
- Device evidence: native build inspected; validator result compared with all B1 content-matched scans

## Preconditions

Use the unmodified current validator and manifest. Do not waive or lower any
threshold.

## Reproduction

Run `python3 Scripts/validate_high_frame_pet.py --manifest Sources/CatAtWork/Resources/DefaultPet.catpet/manifest.json --report /private/tmp/catatwork-b1-rerun-validator.json`
and compare the result with BC-018 through BC-022 and BC-024 through BC-029.

## Expected

An unwaived continuity failure blocks the package; geometry, color, root,
component, pose, identity, cadence, locomotion, async rendering and gaze/body
orthogonality have explicit outcomes.

## Actual

The command exits 0 with `ok: true`, no errors and only three warning messages
(for `bellyPet` and `happy`) while the content-matched B1 scans confirm failures
in eleven current scopes. The validator has no hard checks for most required
metrics.

## Root cause

Confirmed acceptance gap: existing checks and failure policy do not encode the
scoped visual contracts, and warning-bearing output can still be green.

## Impact

Release automation can approve a package whose user-visible behavior is known
to fail.

## Status timeline

- 2026-07-24: validator pass and independent failures bound to the same package
  digest; classified `current-confirmed`.
