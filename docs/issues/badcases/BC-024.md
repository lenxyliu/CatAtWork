# BC-024 — Current production frames contain detached visible components

- Status: open
- Classification: current-confirmed
- ISSUE: ISSUE-021; GitHub #16
- First observed: 2026-07-24
- Affected versions: asset 2026.07.23.6 at commit `0ca2b518d5a079ace1ef1e6d9a6892a5259b080e`
- Fixed versions: none
- Regression test: B1 component scan; no release gate yet
- Automated evidence: QA root `Assets/CatAtWork/qa/b1-current-build-0ca2b518`; scope digest `a42f3141e2e4e6ae3df7fc51a7ec8b9e7b8fa4c41c09611f7e00b349e8ac7836`
- Device evidence: `walkLeft` loop/contact sheet inspected

## Preconditions

Decode current atlas crops losslessly. At alpha ≥ 12, run exact 8-neighbor
connected components and retain secondary components whose visible area is at
least 4 pixels.

## Reproduction

1. Scan every frame crop from all 31 manifest actions.
2. Compare each component with the largest body component.
3. Inspect every hit in the current contact sheet; do not auto-delete pixels.

## Expected

Ordinary body actions contain no unapproved secondary visible component.

## Actual

The scan finds secondary components in **15 frames**: `idleEar` 11/12/18/22,
`walkLeft` 5/22/23, `wave` 15, `petting` 10, `thrown` 12/16,
`bellyRoll` 3/4/5 and `curious` 19. `walkLeft` frame 5 includes a 100-pixel
component with peak alpha 255.

## Root cause

Confirmed asset failure: detached alpha components are present in production
atlas crops. Their authoring provenance is not inferred in B1.

## Impact

Detached material flashes as periodic specks or debris in animation.

## Status timeline

- 2026-07-24: all current action crops scanned; classified
  `current-confirmed`.
