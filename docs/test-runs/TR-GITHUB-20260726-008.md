# TR-GITHUB-20260726-008 — Initial B2 merge-guard false negative

- Status: fail
- Date: 2026-07-26T15:13:00Z
- Content-SHA256: efc7739785aa0dcbbf090f99f367c74250aec49160b11af68022d3925441298a
- Commit/Tree: PR #26 final head `443023ec738c4b449416dcb01e38355a789b9b2e`; base `f829cc2b05a582e5e45b15e6a6d09932fcd66999`
- System: connected GitHub API through Codex JavaScript orchestration
- Xcode: not applicable
- Swift: not applicable
- ISSUE: ISSUE-027; GitHub #14 and #15
- BC: BC-030
- CHG: CHG-20260726-004

## Tested content

The first no-write pre-merge guard for PR #26 after final-head hosted checks,
scope review and pre-merge #14/#15 comments.

## Commands

Fetch the PR, exact-head workflow runs/jobs, all changed filenames, review
submissions and review threads; merge only if every guard field is true.

## Passed

- PR #26 was open, ready, mergeable and still used the expected head and base.
- All 42 filenames remained in scope.
- Hosted `governance` and `swift` jobs remained successful.
- No review thread existed.

## Failed

- The commit-workflow wrapper returned an empty run list during this
  invocation even though direct jobs for run `30207472798` remained
  successful.
- The review-submission wrapper exposed the review under opaque GraphQL ID
  `PRR_kwDOTgz5Hs8AAAABHQfuCQ`, so an incorrect numeric-ID equality test did
  not match review database ID `4782026249`.

## Skipped

- The script exited before the merge call.
- PR #26 and repository state were unchanged.

## Failure analysis

The guard combined two different connector response identities as if they
shared the same shape. The underlying PR, jobs and review were valid; the
orchestration predicates were too strict.

## Evidence

The guard output recorded `workflow: false` and `review: false` while also
showing both successful job records and the exact COMMENTED review body.

## Retrospective

The retry uses authoritative job IDs/run ID plus the numeric review record
from the merged PR comment timeline, and still supplies GitHub's
`expected_head_sha` merge guard.
