# TR-GITHUB-20260726-002 — Closure PR CLI inspection failure

- Status: fail
- Date: 2026-07-26T10:40:39Z
- Content-SHA256: `59c533d8130d64397db65e7f1bc9bc87dff4537101fc246e7e79df173ff9d9d8`
- Commit/Tree: closure head `56490bb414ee8ebbe7db9e4d0fd868b155848b68`
- System: macOS 26.5.2 (25F84), Apple M4 Pro; resumed command environment
- Xcode: 26.6 (17F113)
- Swift: 6.3.3
- ISSUE: ISSUE-015 through ISSUE-026; GitHub #15
- BC: BC-018 through BC-029
- CHG: CHG-20260726-001

## Tested content

The first remote-diff and check-state inspection of closure PR #24 after it was
created from `codex/pet-visual-baseline-closure`.

## Commands

```sh
gh pr view 24 --repo lenxyliu/CatAtWork --json \
  url,isDraft,headRefOid,baseRefOid,changedFiles,additions,deletions,mergeable,mergeStateStatus,statusCheckRollup
gh pr diff 24 --repo lenxyliu/CatAtWork --name-only
```

The invocation also attempted to sort and compare the remote path list with
`git diff --name-only main...HEAD`.

## Passed

- The local committed closure list resolved to the four expected Markdown
  paths.

## Failed

- Both `gh` commands failed with `/bin/bash: gh: command not found`.
- The remote path list was therefore empty and the comparison reported the
  four local paths as missing remotely.

## Skipped

- PR metadata, remote path equality and hosted check state were not verified
  by this invocation.
- No GitHub or repository write was attempted by the failed command.

## Failure analysis

The resumed execution environment did not contain a `gh` binary on `PATH`.
This was a local tool-availability failure, not evidence of a PR diff or GitHub
Actions defect.

## Evidence

The terminal output preserved two `gh: command not found` diagnostics and the
four-line local-versus-empty-remote diff. `command -v gh` subsequently returned
no path.

## Retrospective

The rerun uses the connected GitHub API and has a distinct TR ID.
