# TR-CI-20260723-005 — Final merge preflight and main-push diff contract

- Status: pass
- Date: 2026-07-23T15:02:00+08:00
- Content-SHA256: c4e2a8dcf1abf5acbc0900a480c6d3ca5dcf4326728d8515031f596be47314fc
- Commit/Tree: `codex/ci-swift6-baseline` final working tree before merge
- System: macOS 26.5.2 local; GitHub-hosted macOS 15/Ubuntu
- Xcode: local 26.6 (17F113); hosted 16.4 (16F6)
- Swift: local 6.3.3; hosted 6.1.2; Python 3.13/Pillow 12.3 hosted
- ISSUE: ISSUE-007, ISSUE-008, ISSUE-009, ISSUE-010, ISSUE-013, ISSUE-014
- BC: BC-010, BC-011, BC-012, BC-013, BC-016, BC-017
- CHG: CHG-20260723-002, CHG-20260723-003, CHG-20260723-004, CHG-20260723-005, CHG-20260723-006, CHG-20260723-007, CHG-20260723-008

## Tested content

The final PR head, including CI toolchain/dependencies, evidence lifecycle,
owner-approved unprotected-main policy and incremental push-to-main
governance.

## Commands

```sh
python3 Scripts/check_governance.py --all
python3 -B -m unittest discover -s Scripts/tests -p 'test_*.py' -v
swift build
swift test
bash Scripts/typecheck_without_xcode.sh
python3 Scripts/validate_high_frame_pet.py \
  --manifest Sources/CatAtWork/Resources/DefaultPet.catpet/manifest.json \
  --report /private/tmp/catatwork-final-merge-preflight.json
python3 Scripts/check_governance.py --staged
```

GitHub Actions then runs the PR-base governance job and hosted Swift/package
job on the committed final head.

## Passed

- Governance fixtures: 13 executed, 0 failures/errors.
- Workflow contract requires `github.event.before` on main pushes.
- Local Swift build, 36 unit tests, source smoke and package validation pass.
- Staged and PR-base governance pass with the final governed digest.
- Final hosted `governance` and `swift` jobs pass.

## Failed

The pre-fix `--all` merge preflight deterministically failed CHG-001 freshness
and produced invalid cross-design revision requirements for later CHGs.

## Skipped

Branch protection is unavailable and explicitly accepted as `wont-fix`.
Real-device application acceptance is unrelated to this repository/CI change.

## Failure analysis

`--all` correctly models a new baseline but cannot model an incremental merge.
Using the push event's previous main SHA preserves the accepted-history
boundary already used by PR validation.

## Evidence

- Pre-fix `--all` output in the Codex task.
- Local report: `/private/tmp/catatwork-final-merge-preflight.json`.
- Final Actions run URL added to the PR evidence before merge.

## Retrospective

A green PR is not enough: the workflow triggered by merging needs its own
preflight model. Testing that path before the squash prevented a predictable
red `main`.
