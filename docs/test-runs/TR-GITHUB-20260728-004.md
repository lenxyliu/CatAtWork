# TR-GITHUB-20260728-004 — Foundation color review orchestration failures

- Status: fail
- Date: 2026-07-28T15:04:00Z
- Content-SHA256: b15b804df0d15a771a5fa6a20152b12cd7fcddf91574de5645853cd5807f64ce
- Commit/Tree: PR #32 exact head `23ece48b3c25422a2977ecccfdb92b34fa5662f0`
- System: GitHub.com; gh 2.96.0
- Xcode: hosted 26.6 (17F113)
- Swift: hosted 6.3.3
- ISSUE: ISSUE-016, ISSUE-026; GitHub #10, #21, #15
- BC: BC-019, BC-029
- CHG: CHG-20260728-002

## Tested content

The first two attempts to submit the exact-head procedural review after both
named hosted checks passed.

## Commands

```sh
gh pr checks 32 --required
gh api repos/lenxyliu/CatAtWork/pulls/32/reviews \
  --method POST --input -
```

## Passed

Before both invocations, PR #32 still pointed at exact head
`23ece48b3c25422a2977ecccfdb92b34fa5662f0`, and the named hosted
`governance` and `swift` checks both had `SUCCESS` conclusions.

## Failed

- `gh pr checks --required` returned that no required checks are reported for
  the branch because the private repository uses procedural controls rather
  than a GitHub branch-protection required-check set.
- The first review POST supplied plain text to `--input -`, which expects a
  complete JSON request body, and GitHub returned HTTP 400 `Problems parsing
  JSON`.

## Skipped

No review or merge occurred through either failed invocation.

## Failure analysis

The first failure selected the wrong CLI filter; the workflow's two named
checks were successful but not configured as a branch-protection set. The
second failure used the wrong `gh api` input mode. The corrected invocation
asserted the two named conclusions directly and supplied `event` and `body`
as form fields.

## Evidence

The exact failed output is summarized above. Neither failure changed the PR
head, checks, files, comments or merge state.

## Retrospective

Procedural repositories should assert the required workflow names and use
typed `gh api -f` fields for review creation.
