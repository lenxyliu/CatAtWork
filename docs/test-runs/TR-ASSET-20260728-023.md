# TR-ASSET-20260728-023 — Detail-preserving foundation color acceptance

- Status: pass
- Date: 2026-07-28T14:45:33Z
- Content-SHA256: b15b804df0d15a771a5fa6a20152b12cd7fcddf91574de5645853cd5807f64ce
- Commit/Tree: `codex/default-pet-visual-foundation-color-correction` based on `4011d2d8c8fc4559ee1da911a8b9b29fd720d16f`
- System: macOS 26.5.2 (25F84), Apple M4 Pro; Python 3.9.6; Pillow 11.3.0
- Xcode: 26.6 (17F113)
- Swift: 6.3.3
- ISSUE: ISSUE-016, ISSUE-026; GitHub #10, #21
- BC: BC-019, BC-029
- CHG: CHG-20260728-001

## Tested content

The second 216-frame foundation correction using the fixed
source-effect-detail-preserving material rule, the source-effect color/detail
oracle, exact installed production frames, two independent partial package
builds and native manifest
`b9434a2c9fa4eeb02b2ba36e0ae2db29af37f8c2a9eddcded2464fb1bb5bc9c6`.

## Commands

```sh
/usr/bin/python3 -B Scripts/validate_foundation_color_oracle.py ...
/usr/bin/python3 -B Scripts/validate_foundation_assets.py ...
/usr/bin/python3 -B Scripts/compare_foundation_color_only.py ...
/usr/bin/python3 -B Scripts/build_foundation_qa.py ...

/usr/bin/python3 -B -m unittest Scripts.tests.test_foundation_assets
/usr/bin/python3 -B -m unittest discover -s Scripts/tests -p 'test_*.py'
swift test --build-path <independent-fresh-swift-retry-path>

/usr/bin/python3 -B Scripts/assemble_foundation_assets.py build ...
/usr/bin/python3 -B Scripts/build_catpet.py --require-all ...
diff -qr <build-a.catpet> <build-b.catpet>

git lfs install --local --skip-repo
git check-attr filter -- Assets/CatAtWork/frames/<scoped-action>/<frame>.png
git diff --check
python3 Scripts/check_governance.py digest
```

TR-ASSET-20260728-022 separately preserves the failed fresh Swift,
wrong-draft-root and overly strict branch-ref invocations.

## Passed

- The accepted source-effect oracle passed 12/12 checks. It changed
  67,907/68,749 visible pixels; material ΔE00 is `0.826423` light,
  `2.947122` warm and `0.389036` dark. Same-material L* gradient-retention
  ratios are `1.010780`, `0.990451` and `1.013864`, all within
  `[0.98, 1.05]`. Report SHA-256:
  `1c30b02c7fda652cf8f7a97ba5288b48ad7aac788e75120d3d98913a43128cd1`.
- Foundation validation passed 3,802/3,802 B2 observations and 15/15 custom
  checks. The fresh double-build validation report SHA-256 is
  `10b12d9a4cc590301b13029a5b6d1890fbe446d8fb1f75acb4c2b6d518e8a8a3`.
- The production color-only comparison passed all 216 frames with
  10,680,217 changed visible RGB pixels, zero alpha changes, zero hidden-RGB
  changes and zero violations. Report SHA-256:
  `842010b7724b7d2371852826fd990ef404d0749ad84d4ce3019dc1f75182a290`.
- Both fresh builds have source tree digest
  `b4049c99c9c4135bf2ee039ecc3447879a9ab6b4df50a4843607ee90db8809`,
  package tree digest
  `0bf61577b6cf57cc3b0737aff9bc2e108b9e2f9170bd408401e180289d3d302a`
  and manifest SHA-256
  `b9434a2c9fa4eeb02b2ba36e0ae2db29af37f8c2a9eddcded2464fb1bb5bc9c6`.
  Their package trees are byte-identical, and all 216 fresh frames equal the
  installed production targets.
- QA produced 27 governed outputs for nine actions and five loop repetitions;
  report SHA-256 is
  `af872b696c5d8a52fc7025e3f7ac91f9bee8d9963f81a9d8a8bbc4b3ceba7f9b`.
- Targeted foundation Python passed 6/6, full Python passed 43/43 and the
  independent fresh-path Swift retry passed 64/64.
- All 216 production PNG targets report `filter=lfs`. `git lfs install`
  retained the repository's custom executable path and returned only its
  existing normalization warning.
- Only the nine scoped action directories differ from base. The animation
  spec remains
  `f0e3ade578a13ed41003f6d512a81e8cce7852a2de3f219066ecd68758309624`;
  generated atlases and the runtime package are unchanged, and the runtime
  manifest remains
  `ae4c8d8abef4f22ca6c7636dd3c6a0f72f66a1c42d75deda0c26f49636e6f393`.
  The runtime high-frame validator remains `ok: true` with 31 animations,
  16 look directions and zero errors.
- The interaction asset checkpoint
  `31d8ab14540e856c381613f98f29b540c472ebb8` remains an ancestor of the
  documentation-only handoff head, with no product path changes after it.
- Native requests at `13:03:54Z`, `13:04:26Z`, `13:04:43Z`,
  `13:05:04Z` and `13:05:21Z` covered at least five loops each of `idle`,
  `walkLeft`, `walkRight`, `runLeft` and `runRight`; request-log SHA-256 is
  `ec5c8b99de0c25dc82584f34b90c8cce9228db0658f7db4f52e4f8f732027951`.
  The user then explicitly accepted the color in the exact native instance.
- The user-requested rejected-instance PID 38531 closed, while the accepted
  detail-preserving PID 96878 remained running during acceptance attribution.
- `git diff --check` passed. The governed content digest is the recorded
  `b15b804df0d15a771a5fa6a20152b12cd7fcddf91574de5645853cd5807f64ce`.

## Native scope observations

The user also observed a fast blink and inconsistent size between actions.
These are not color acceptance failures. Cross-action size/proportion is the
existing GitHub #11 / ISSUE-017 / BC-020 scope. Blink speed is a non-color
timing/animation-semantics observation; GitHub #18 covers display-cadence
quantization, but attribution to that clock versus authored blink duration
requires a separate content-matched timing reproduction. This batch changes
no alpha, geometry, scale, pose, frame order, animation spec or runtime clock
and therefore does not claim either observation fixed.

## Failed

None in this acceptance execution. TR-ASSET-20260728-019 through
TR-ASSET-20260728-022 retain all earlier reproduction, invocation, detail and
fresh-path failures.

## Skipped

Interaction frame installation or resumption; physical actions; generated
atlas/runtime-package integration; B5–B8; release, tag, DMG and GitHub Release.

## Failure analysis

None. The independent Swift retry and corrected deterministic paths passed
without a product-code or asset change.

## Evidence

Generated reports, double builds, QA and native harness products remain
outside Git. Their stable hashes and content decisions are recorded above;
private screenshots are not published.

## Retrospective

Direct color acceptance must remain separate from later timing and
cross-action-scale acceptance. A color-only prerequisite can preserve those
open defects without silently expanding its implementation scope.
