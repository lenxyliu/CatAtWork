# TR-BASELINE-20260723-002 — Automated source, package and candidate baseline

- Status: pass
- Date: 2026-07-23T10:54:20+08:00
- Content-SHA256: f65994c801ab82bdf44dadba1e8fb53e1b78864006457c57d00afe8e0911ab10
- Commit/Tree: unborn `main`; pre-clean-baseline working tree
- System: macOS 26.5.2 (25F84), Apple silicon
- Xcode: 26.6 (17F113)
- Swift: 6.3.3 (swiftlang-6.3.3.1.3)
- ISSUE: ISSUE-005, ISSUE-006
- BC: BC-008, BC-009
- CHG: CHG-20260723-001

## Tested content

Current Swift core/app build, 36 existing unit tests, independent Swift 6 typecheck/core smoke, canonical default `.catpet`, ZIP package/install path, and historical build 8 DMG. Governed content matches the recorded digest.

## Commands

```sh
swift test
swift build
bash Scripts/typecheck_without_xcode.sh
python3 -B Scripts/validate_high_frame_pet.py --manifest Sources/CatAtWork/Resources/DefaultPet.catpet/manifest.json --report /private/tmp/catatwork-high-frame-report.json
bash Scripts/package_catpet.sh Sources/CatAtWork/Resources/DefaultPet.catpet /private/tmp/catatwork-baseline.catpet
/private/tmp/catatwork-typecheck/pet-store-smoke /private/tmp/catatwork-baseline.catpet /private/tmp/catatwork-pet-store-smoke
bash Scripts/verify_dmg.sh Build/Release/猫上班了-1.0.3-构建8-抛掷动画回退版.dmg
```

The smoke executable was compiled from `CatAtWorkCore`, `LegacyCodexV2Importer.swift`, `PetStore.swift`, and `pet_store_smoke.swift` against the Xcode 26.5 SDK using a temporary dylib/rpath under `/private/tmp/catatwork-typecheck`.

## Passed

- `swift test`: 36 executed, 0 failures (24 behavior/physics + 12 manifest/layout/import tests).
- `swift build`: debug build passed.
- Independent Swift 6 typecheck and core smoke: passed using the canonical runtime package.
- High-frame validation: `ok=true`, 31 animations, 16 look directions, `bodyScale=[1.0]`, no errors; safe canvas 665×737.
- Canonical package ZIP creation and ZIP installation smoke: passed.
- Build 8 DMG: system checksum valid, read-only mount structure present, nested/App signature verification passed, `arm64` and `x86_64` present.

## Failed

None in this execution.

## Skipped

- Legacy Codex v2 import smoke: no `pet.json` legacy fixture exists in the repository.
- Formal `verify_release.sh`: build 8 is an ad-hoc test candidate and intentionally lacks production Bundle ID, Sparkle key/feed, Developer ID and notarization credentials.
- Real-device interaction/permission/display scenarios: require a separate device TR and video/log evidence.

## Failure analysis

An intermediate attempt to link the PetStore smoke against a temporary static Swift library omitted public Swift symbols. Rebuilding the same source as a temporary dylib with an explicit rpath succeeded; this was test-harness linkage, not an application failure.

The asset validator retains two non-failing temporal warnings (`bellyPet`, `happy`) and reports 24 frames that the old fixed canvas would clip. These are preserved as device-review targets; the current safe canvas reports no validation errors.

## Evidence

- `/private/tmp/catatwork-high-frame-report.json`
- Test/build/typecheck/import/DMG command output in this Codex task
- DMG SHA-256 in `docs/recovery/PREHISTORY-MANIFEST.md`

Temporary paths are execution evidence locations, not durable attachments. A release candidate audit must copy required logs/video to its immutable evidence location.

## Retrospective

The frozen root audit's 36-test claim is reproducible, but it does not cover the four newly identified design defects or App-layer integration. Build 8 is structurally valid as a test candidate; it is not a production release. Legacy-import fixture coverage and real-device acceptance remain explicit gaps rather than being treated as passes.

