# TR-CI-20260723-002 — Hosted Pillow dependency resolution and local validation

- Status: pass
- Date: 2026-07-23T14:26:47+08:00
- Content-SHA256: 23eb92b23c77f345942aa758f9a15854119607616720073b976364c4be12fdd0
- Commit/Tree: `codex/ci-swift6-baseline` working tree after run 29984822374
- System: macOS 26.5.2, Apple silicon
- Xcode: 26.6 (17F113)
- Swift: 6.3.3 (swiftlang-6.3.3.1.3); local Python 3.9/Pillow 11.3.0
- ISSUE: ISSUE-009
- BC: BC-012
- CHG: CHG-20260723-004

## Tested content

The hosted validation environment declaration: Python 3.13,
`Scripts/requirements-validation.txt`, pinned Pillow 12.3.0, installation
step, and Python/Pillow identity output.

## Commands

```sh
python3 -m pip download \
  --only-binary=:all: \
  --platform macosx_11_0_arm64 \
  --python-version 3.13 \
  --implementation cp \
  --abi cp313 \
  --no-deps \
  --dest /private/tmp/catatwork-pillow-wheel-check \
  --requirement Scripts/requirements-validation.txt
python3 -c 'import PIL; print("local Pillow", PIL.__version__)'
python3 Scripts/validate_high_frame_pet.py \
  --manifest Sources/CatAtWork/Resources/DefaultPet.catpet/manifest.json \
  --report /private/tmp/catatwork-ci-python-high-frame-report.json
python3 -B -m unittest discover -s Scripts/tests -p 'test_*.py' -v
python3 Scripts/check_governance.py digest
```

## Passed

- PyPI resolved the exact
  `pillow-12.3.0-cp313-cp313-macosx_11_0_arm64.whl` required by the hosted
  environment.
- Downloaded wheel SHA-256:
  `d69141514cc30b774ceea5e3ed3a6635c8d8a96edf664689b890f4089111fb35`.
- High-frame package validation returned `ok: true` with no errors using the
  local installed Pillow 11.3.0.
- Governance fixtures: 10 executed, 0 failures/errors.
- Governed digest:
  `23eb92b23c77f345942aa758f9a15854119607616720073b976364c4be12fdd0`.

## Failed

None in this local dependency-resolution run.

## Skipped

The exact Python 3.13/Pillow 12.3.0 execution remains delegated to the hosted
Actions rerun. The local Apple Python is 3.9 and was not mutated.

## Failure analysis

Previous run `29984822374` failed only because Pillow was undeclared. The
required arm64 CPython 3.13 wheel exists and the validator succeeds when
Pillow is present, so provisioning the declared environment directly addresses
the root cause.

## Evidence

- Failed hosted package step: Actions run `29984822374`.
- Downloaded wheel:
  `/private/tmp/catatwork-pillow-wheel-check/pillow-12.3.0-cp313-cp313-macosx_11_0_arm64.whl`.
- Validation report:
  `/private/tmp/catatwork-ci-python-high-frame-report.json`.

## Retrospective

Validation dependencies are part of the executable test contract. Pinning
only the application toolchain is insufficient when auxiliary Python scripts
can otherwise inherit undocumented developer or runner packages.
