# TR-ASSET-20260728-022 — Foundation color final-verification failed invocations

- Status: fail
- Date: 2026-07-28T14:35:55Z
- Content-SHA256: b15b804df0d15a771a5fa6a20152b12cd7fcddf91574de5645853cd5807f64ce
- Commit/Tree: `codex/default-pet-visual-foundation-color-correction` based on `4011d2d8c8fc4559ee1da911a8b9b29fd720d16f`
- System: macOS 26.5.2 (25F84), Apple M4 Pro; Python 3.9.6; Pillow 11.3.0
- Xcode: 26.6 (17F113)
- Swift: 6.3.3
- ISSUE: ISSUE-016, ISSUE-026; GitHub #10, #21
- BC: BC-019, BC-029
- CHG: CHG-20260728-001

## Tested content

The user-accepted detail-preserving 216-frame foundation color rebuild and
its final fresh-path Swift, deterministic-build and unchanged-scope proofs.

## Commands

```sh
swift test --build-path <fresh-swift-path>

/usr/bin/python3 -B Scripts/assemble_foundation_assets.py build \
  --draft-root <generation-root> \
  --contract Config/DefaultPet/foundation-contract.json \
  --output-root <fresh-build-root>

test "$(git rev-parse codex/default-pet-visual-interaction)" = \
  "31d8ab14540e856c381613f98f29b540c472ebb8"
```

Each corrected rerun used a new path or a corrected read-only assertion.
No failed output was overwritten or used as passing evidence.

## Passed

- Targeted foundation Python passed 6/6 and the full Python suite passed
  43/43 before the Swift execution.
- The fresh Swift build completed and 63/64 test cases passed.
- The LFS attribute scan had already confirmed all 216 scoped PNGs use
  `filter=lfs`.

## Failed

- `testCacheHitsAndCoalescesConcurrentLoads` again observed loader counts
  `0` and `2` where the two assertions expected `1`. This is the same
  timing-sensitive cache-test signature retained in
  TR-ASSET-20260728-020.
- The first deterministic rebuild addressed the generation parent rather
  than its `extracted` draft directory and failed closed on missing
  `idle.json`.
- The first interaction-preservation assertion expected the branch ref itself
  to equal the asset checkpoint. The branch instead points at documentation
  handoff commit `ae6e435c4fbab2d80e9c569f4049b3109758a8a3`,
  whose parent is the required
  `31d8ab14540e856c381613f98f29b540c472ebb8` checkpoint.

## Skipped

No failed invocation advanced staging, publication, Issue status, interaction,
physical, integration, B5–B8 or release work.

## Failure analysis

The Swift failure is the unchanged concurrent-cache timing race already
demonstrated to pass on an independent fresh path without a code change. The
other two failures are command/assertion errors: the governed draft files are
under `extracted`, and the interaction branch has a documentation-only child
commit after the preserved asset checkpoint. Neither failure changes or
implicates the corrected foundation pixels.

## Evidence

The failed Swift build and empty failed deterministic output remain in the
fresh final-verification root. Git history proves that the interaction
branch's only child changes after `31d8ab14540e856c381613f98f29b540c472ebb8`
are CHG/plan/handoff/TR Markdown.

## Retrospective

Fresh-path retries remain necessary for the concurrent cache test, and
checkpoint preservation should compare ancestry plus governed product paths,
not require a documentation handoff ref to equal its parent.
