# TR-ASSET-20260728-020 — Foundation color correction failed invocations

- Status: fail
- Date: 2026-07-28T12:12:05Z
- Content-SHA256: ad3fd92906e66c3347a98c6ff6a8717e4a157509bc6f61344277be227b7796fd
- Commit/Tree: `codex/default-pet-visual-foundation-color-correction` based on `4011d2d8c8fc4559ee1da911a8b9b29fd720d16f`
- System: macOS 26.5.2 (25F84), Apple M4 Pro; Python 3.9.6; Pillow 11.3.0
- Xcode: 26.6 (17F113)
- Swift: 6.3.3
- ISSUE: ISSUE-016, ISSUE-026; GitHub #10, #21
- BC: BC-019, BC-029
- CHG: CHG-20260728-001

## Tested content

The corrected foundation contract, deterministic source-effect color oracle,
color-only diff gate and rebuilt 216-frame source slice before native user
acceptance.

## Commands

```sh
/usr/bin/python3 -B Scripts/validate_foundation_color_oracle.py \
  --source-effect-sheet <effect-sheet> \
  --source-effect-frame \
    /private/tmp/catatwork-b4-interaction-generation.OfpeFC/extracted/waiting/000.png \
  --foundation-contract Config/DefaultPet/foundation-contract.json \
  --report <report>

swift test --build-path \
  /private/tmp/catatwork-b4-foundation-color-swift.nFdTjR

/usr/bin/python3 -B Scripts/validate_foundation_assets.py \
  --candidate-root <candidate> \
  --package-manifest <candidate.catpet/manifest.json> \
  --foundation-contract Config/DefaultPet/foundation-contract.json \
  --b2-contract <contract> \
  --report <report>
```

The first nested double-build orchestration was still packaging when a
diagnostic retry addressed its staging path. Every retry used a new evidence
path; no failed report was overwritten.

## Passed

- The first fresh Swift path built the complete package and 63 of 64 XCTest
  cases passed.
- The post-install color-only diff completed before the oracle-path error and
  passed all 216 frames with zero alpha, size, ICC or hidden-RGB changes.
- The first deterministic package was ultimately produced with manifest
  SHA-256
  `0999c88d7c0c3e3c39c313c0028a6e90ca7b8c83fe603248690e5eed6e2a1be8`.

## Failed

- One oracle invocation addressed nonexistent
  `/private/tmp/catatwork-b4-foundation-generation.rdcnQP/waiting.png` and
  raised `FileNotFoundError`. The frozen sheet is under the preserved
  interaction generation root.
- The first fresh Swift execution failed two assertions in unchanged
  `testCacheHitsAndCoalescesConcurrentLoads`: observed loader counts `0` and
  `2` where both assertions expected `1`.
- A diagnostic package retry encountered the still-active first job's
  `.candidate.catpet.staging`.
- The first final double-build validation addressed nonexistent
  `Config/DefaultPet/visual-qa-acceptance.json` and raised
  `FileNotFoundError`. The accepted B2 contract is
  `Tests/Fixtures/VisualQA/default-pet-b1-contract.json`.
- The first native QA bundle launch exited with `OS_REASON_DYLD` because the
  freshly copied debug executable did not yet contain the app-bundle
  `@executable_path/../Frameworks` rpath for Sparkle.

## Skipped

No failed invocation was used as acceptance evidence. Native user acceptance,
staging, GitHub publication and merge remained pending.

## Failure analysis

The two missing paths, staging collision and native rpath omission were
command/harness errors, not product findings. The disposable native harness
passed strict deep code-signature verification and launched after adding the
same framework rpath present in the earlier accepted review app. The Swift
cache test is timing-sensitive: a second independent fresh-path execution
passed all 64 cases without code changes. All failures are preserved here so
the later passing TR can cite new, content-matched reruns.

## Evidence

The failed Swift build remains at
`/private/tmp/catatwork-b4-foundation-color-swift.nFdTjR`. The failed/retried
determinism roots remain at
`/private/tmp/catatwork-b4-foundation-color-determinism.opoqEV` and
`/private/tmp/catatwork-b4-foundation-color-determinism-final.qGf6W3`.
Oracle and post-install evidence remains at
`/private/tmp/catatwork-b4-foundation-postinstall.fE75jJ`.

## Retrospective

Long-running package validation should be awaited as one owned process, and
every governed input path should be copied from the accepted command record.
