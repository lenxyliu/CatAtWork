# TR-RECOVERY-20260723-002 — Remote round-trip and authorized cleanup

- Status: pass
- Date: 2026-07-23T15:12:00+08:00
- Content-SHA256: c4e2a8dcf1abf5acbc0900a480c6d3ca5dcf4326728d8515031f596be47314fc
- Commit/Tree: main 340251a plus local cleanup state
- System: macOS 26.5.2, Apple silicon; private GitHub Release
- Xcode: not applicable
- Swift: 6.3.3; Git LFS 3.7.0; GitHub CLI 2.96.0
- ISSUE: ISSUE-005
- BC: BC-008
- CHG: CHG-20260723-009

## Tested content

All recovered prehistory attachments, representative remote downloads,
workspace/frozen legacy Git equivalence, duplicate runtime package identity
and the post-cleanup clean repository.

## Commands

```sh
gh release view prehistory-2026-07-23 --repo lenxyliu/CatAtWork
shasum -a 256 <local-and-downloaded-attachments>
unzip -tqq <downloaded-assets-archives>
rsync -nrc --delete --itemize-changes \
  .git/ /Users/oops/Documents/CatAtWork-Recovery-2026-07-23/legacy-git/
diff -qr Resources/DefaultPets/CatAtWork.catpet \
  Sources/CatAtWork/Resources/DefaultPet.catpet
git status --short --branch
git lfs status
git fsck --full
```

## Passed

- Private Release contains nine Assets ZIPs and seven DMGs.
- All 16 server SHA-256 digests match local artifacts.
- Oldest, largest and latest/current ZIPs plus build 8 DMG round-trip hashes
  match; all downloaded ZIPs pass integrity testing.
- Workspace legacy Git and frozen backup have no checksum differences.
- Duplicate package has 34 matching files and identical aggregate SHA-256.
- Owner approved all four exact cleanup targets.
- Post-cleanup `.git` is 158 MiB; `.build`, `Build` and duplicate package do
  not exist.
- Local/remote main both equal `340251a`; Git and LFS are clean.
- `git fsck --full` reports no errors.

## Failed

None.

## Skipped

The frozen 2.2 GiB `legacy-git`, nine local ZIPs, seven local DMGs and remote
Release were intentionally retained. They are recovery evidence, not routine
workspace duplicates.

## Failure analysis

No data-integrity failure occurred. The original capacity was caused by loose
historical binary objects plus regenerable builds and a duplicate runtime
package, not by the clean source history.

## Evidence

- Release:
  `https://github.com/lenxyliu/CatAtWork/releases/tag/prehistory-2026-07-23`.
- Manifest: `docs/recovery/PREHISTORY-MANIFEST.md`.
- Frozen checksum manifest:
  `/Users/oops/Documents/CatAtWork-Recovery-2026-07-23/legacy-git-files.sha256`.
- Remote round-trip directory:
  `/private/tmp/CatAtWork-Release-Roundtrip-20260723`.

## Retrospective

The deletion gate prevented loose objects from being mistaken for disposable
cache. After history was exported and independently downloadable, the same
workspace could safely shrink while retaining both a frozen forensic copy and
portable remote archives.
