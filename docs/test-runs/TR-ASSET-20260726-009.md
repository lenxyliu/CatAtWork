# TR-ASSET-20260726-009 — Staging-path fixture reuse failure

- Status: fail
- Date: 2026-07-26T18:02:00Z
- Content-SHA256: ddeb28c7b8513645f50005f08dc65d26188fec65c17bc1b8c1ff21a107de686f
- Commit/Tree: `codex/canonical-pet-asset-contract`; uncommitted canonical no-overwrite refinement
- System: macOS 26.5.2 (25F84), Apple M4 Pro; Python 3.9.6; Pillow 11.3.0
- Xcode: 26.6 (17F113)
- Swift: 6.3.3
- ISSUE: ISSUE-015, ISSUE-017, ISSUE-018, ISSUE-021, ISSUE-022, ISSUE-026
- BC: BC-018, BC-020, BC-021, BC-024, BC-025, BC-029
- CHG: CHG-20260726-005

## Tested content

The canonical builder after changing format-2 output publication to refuse an
existing destination and build through a distinct staging path.

## Commands

```sh
python3 -B -m unittest Scripts.tests.test_asset_contract
```

## Passed

14 of 15 tests completed without an assertion failure.

## Failed

`test_disconnected_components_require_one_bounded_review` errored before its
reviewed-exception assertion because it reused `result.catpet` after the
deliberate first failure left `.result.catpet.staging`.

## Skipped

The suite stopped with one error; no result is used as acceptance evidence.

## Failure analysis

The production code correctly refused to reuse a non-empty staging target.
The synthetic test conflated two independent build attempts by assigning them
the same output name. The fixture must use a fresh destination for the
reviewed-exception attempt.

## Evidence

The runner completed 15 tests in 0.431 seconds and reported one error:
`CanonicalAssetContractError: canonical staging path already exists:
.result.catpet.staging`.

## Retrospective

Keep the no-overwrite behavior. Give each intentional failing/passing build
attempt a unique temporary output and rerun under a new TR.
