# TR-ASSET-20260727-004 — Git LFS initialization and governed-hook verification

- Status: pass
- Date: 2026-07-27T15:04:00Z
- Content-SHA256: 0ba6eb93d724560e3499a0b2805084061a14c5022484f5982e8dff3976e069b8
- Commit/Tree: `codex/default-pet-visual-foundation` based on
  `36b033b28677403821e6ccefb96838b3f58c140f`
- System: macOS 26.5.2 (25F84), Apple M4 Pro; Git 2.50.1; Git LFS 3.7.0
- Xcode: 26.6 (17F113)
- Swift: 6.3.3
- ISSUE: ISSUE-015, ISSUE-016, ISSUE-017, ISSUE-018, ISSUE-021,
  ISSUE-022, ISSUE-026
- BC: BC-018, BC-019, BC-020, BC-021, BC-024, BC-025, BC-029
- CHG: CHG-20260727-002

## Tested content

Git LFS filter initialization and the existing governed pre-push integration
before any production raster change or stage.

## Commands

```sh
git config --show-origin --get-regexp '^filter\.lfs\.'
git config --get core.hooksPath
sed -n '1,220p' .githooks/pre-push
git lfs env
git lfs install --skip-repo
git check-attr filter Assets/CatAtWork/identity/canonical-turnaround.png
git check-attr filter Assets/CatAtWork/frames/idle/000.png
```

## Passed

- Git LFS clean, smudge and process filters are configured with
  `filter.lfs.required=true`.
- `core.hooksPath` is `.githooks`.
- `.githooks/pre-push` runs repository governance and then invokes
  `git-lfs pre-push`.
- `git lfs install --skip-repo` returned `Git LFS initialized.` without
  changing the governed hook.
- Production identity and frame PNG patterns resolve to `filter: lfs`.

## Failed

None.

## Skipped

No production raster was staged. Every final raster target will receive an
explicit `git check-attr filter <path>` check before staging.

## Failure analysis

None. The non-overwriting recovery from TR-ASSET-20260727-003 preserves both
repository governance and Git LFS behavior.

## Evidence

The configuration and hook output is preserved in the primary Codex task
transcript.

## Retrospective

The repository’s composite hook is the correct integration point; production
LFS proof still must be enumerated over all changed raster targets.
