# TR-ASSET-20260727-005 — Frozen B4 foundation contract

- Status: pass
- Date: 2026-07-27T15:25:00Z
- Content-SHA256: f8e28aee1f6165d977fa74e3e8f7ea6ae04bd945b8715b8f26b95b7ba8f7c548
- Commit/Tree: `codex/default-pet-visual-foundation` based on
  `36b033b28677403821e6ccefb96838b3f58c140f`
- System: macOS 26.5.2 (25F84), Apple M4 Pro; Python 3.9.6; jq
- Xcode: 26.6 (17F113)
- Swift: 6.3.3
- ISSUE: ISSUE-015, ISSUE-016, ISSUE-017, ISSUE-018, ISSUE-021,
  ISSUE-022, ISSUE-026
- BC: BC-018, BC-019, BC-020, BC-021, BC-024, BC-025, BC-029
- CHG: CHG-20260727-002

## Tested content

`Config/DefaultPet/foundation-contract.json` before any B4 production raster
change.

## Commands

```sh
jq -e '
  .schemaVersion == "catatwork.foundation/v1" and
  .status == "frozen" and
  .scope.frameCount == 216 and
  (.scope.actions | length) == 9 and
  .canonicalScale.pixelsPerBodyUnit == 220 and
  (.componentPolicy.exceptions | length) == 0
' Config/DefaultPet/foundation-contract.json

jq -n \
  --slurpfile foundation Config/DefaultPet/foundation-contract.json \
  --slurpfile b2 Tests/Fixtures/VisualQA/default-pet-b1-contract.json \
  '$foundation[0].deformationBudgets.micro ==
     $b2[0].thresholdProfiles.micro and
   $foundation[0].deformationBudgets.general ==
     $b2[0].thresholdProfiles.general and
   $foundation[0].deformationBudgets.locomotion ==
     $b2[0].thresholdProfiles.locomotion and
   $foundation[0].identityBudgets.endpointHeadScaleRelativeDeviation ==
     $b2[0].identityPolicy.headScaleDeviation and
   $foundation[0].identityBudgets.endpointDarkSupportRelativeDeviation ==
     $b2[0].identityPolicy.darkSupportDeviation'

ACTUAL_SHA=$(shasum -a 256 \
  Assets/CatAtWork/identity/canonical-turnaround.png | awk '{print $1}')
FROZEN_SHA=$(jq -r '.identitySheet.sha256' \
  Config/DefaultPet/foundation-contract.json)
test "$ACTUAL_SHA" = "$FROZEN_SHA"

python3 Scripts/check_governance.py digest
git diff --check
```

## Passed

- The contract freezes exactly nine actions and 216 frames.
- The identity reference binds all four turnaround views and their landmarks,
  material ROIs and alpha bounds to
  `57cc330d4b7cc415f48d06fc01e5350dc47cf7b2d99b50afcd305e6a6203f6d4`.
- Scale is exactly 220 px/body-unit; action/frame overrides and packaging
  resampling are forbidden.
- Canvas is exactly 665×737 RGBA8 straight-alpha sRGB with a 16 px safe
  margin.
- Anatomical root and support line are frozen at `(332.5, 537)`.
- Micro, general and locomotion deformation budgets and identity budgets
  exactly equal the accepted B2 values.
- No disconnected-component exception is approved.
- The governed-content digest is
  `f8e28aee1f6165d977fa74e3e8f7ea6ae04bd945b8715b8f26b95b7ba8f7c548`.

## Failed

None.

## Skipped

Production frames, partial package output and generated QA evidence do not
exist yet and were not evaluated by this contract-only checkpoint.

## Failure analysis

None.

## Evidence

The durable evidence is the governed foundation contract itself. Command
output is preserved in the primary Codex task transcript.

## Retrospective

The foundation slice now has one reviewable source for identity, scale, root,
support, pose, material, action semantics and deformation budgets before art
generation begins.
