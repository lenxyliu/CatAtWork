# 猫上班了：安装、权限与卸载

## 安装

1. 打开 `猫上班了.dmg`。
2. 将“猫上班了.app”拖入“应用程序”。
3. 从“应用程序”首次打开。正式发布版会使用 Developer ID 签名并完成 Apple 公证；开发测试版仅供本机验收。
4. 菜单栏爪印是永久入口，可显示小猫、调整大小、打开设置、导入 `.catpet`、查看动作和诊断日志。

应用首次运行即内置默认 IP“猫上班了”，不需要另行下载素材。

## 可选权限

- 辅助功能：仅用于把前台窗口标题即时归类为工作、浏览、影音、会议或游戏；原始标题不保存。
- 输入监控：用于在其他应用上方识别全局键鼠活跃度和完整悬停手势；不记录具体按键。拒绝后，自主行为、点击、拖拽、右键以及猫身范围内的基础悬停/抚摸仍可用。
- 自动化：仅在用户主动开启音乐感知后，分别读取 Apple Music/Spotify 的播放或暂停状态；不读取曲名。

三项权限互相独立，可随时在“系统设置 → 隐私与安全性”撤销。完全拒绝权限时，基础桌面宠物仍可运行。

## 更新

正式发行包通过 Sparkle 2 检查签名更新；每个更新归档同时需要 Apple 代码签名、公证和 Sparkle EdDSA 签名。开发测试版不会连接占位更新地址。

## 卸载

1. 从菜单栏爪印选择“猫下班了”。
2. 将“应用程序”中的“猫上班了.app”移到废纸篓。
3. 如需同时清除本机宠物包、设置和诊断记录，可在 Finder 的“前往文件夹”中检查并自行移除：
   - `~/Library/Application Support/猫上班了`
   - `~/Library/Logs/猫上班了`
   - `~/Library/Preferences/<正式 Bundle ID>.plist`
4. 在“系统设置 → 隐私与安全性”中移除已经授予的辅助功能、输入监控或自动化权限。

应用没有账号、云端数据或服务器侧用户记录。
