# 隐私说明

猫上班了不创建账号，不包含遥测、广告 SDK 或云同步。

- 前台应用：仅使用 bundle identifier 决定宠物行为。
- 窗口标题：若用户授予辅助功能权限，标题只在内存中归类；原始内容不会写入日志、偏好设置或文件。
- 键盘：只统计活跃时间，不读取或保存按键内容。
- 音乐应用：仅在用户启用时读取播放/暂停状态。
- 诊断日志：为了定位动作误触发、重复落地和跳帧，应用在本机
  `~/Library/Logs/猫上班了/interaction.jsonl` 记录粗粒度的事件名、触发来源、
  动作切换、队列长度、帧号，以及工作类型/活跃状态/音乐状态/充电状态/
  音量档位/昼夜档位。不记录具体按键、窗口标题、文件内容、曲名、精确音量或鼠标精确轨迹。
  日志达到 2 MB 后仅保留一份轮换备份，不上传；用户可从爪印菜单打开并随时删除。
- 所有设置保存在当前 Mac。

撤销权限后，应用自动退化为基础桌面宠物。
