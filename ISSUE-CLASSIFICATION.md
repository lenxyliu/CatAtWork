# 猫上班了：当前问题分类与修复状态

更新日期：2026-07-23。状态只指当前源码和素材工作区；未标记“已重建并实机验收”的项目，不视为用户桌面上已生效。

## 1. 图像帧与角色素材

| 问题 | 证据/根因 | 状态 |
| --- | --- | --- |
| `landing` 旧序列出现二次伏地和姿势倒退 | 旧 24 帧的 8→9、16→17 连接不连续 | **已替换并验证**：正式包使用 24 张编号帧，物理层只发送一次落地事件；仍待实机抛掷链验收 |
| `thrown` 空中翻滚有耳朵/尾巴碎片和方向跳变 | 某些旧 imagegen 母图实际只有 7 个姿势，旧脚本硬切为 8 张 | **已整组重做并替换**：三组各 8 姿势，正式包 24 帧；8→9、16→17 位移分别为 0.0549、0.0593 身体单位 |
| `jump` 和 `startled` 有错误伏地帧、比例突变 | 旧中间段母图数量不足或连续性不足 | **已整组重做并替换**：均为 imagegen 24 个有效姿势，段间连续性检查通过 |
| 边缘绿色、毛发被裁、宽姿势被切 | 固定横向分槽会把跨越分槽的身体切开 | **打包与渲染路径已修复**：可变画布恢复 `sourceSize/trimRect/pivot`，当前安全运行画布 665×761；全动作联系表未见边缘裁切，待桌面实拍复核 |
| 桌面实际毛色比生成图深、瞳色过蓝 | 渲染与源素材调色不一致 | 已使用一套不改 alpha/尺寸的保守色调 LUT；待新包桌面实拍复核 |

## 2. 动作触发、抢占和状态机

| 问题 | 根因 | 状态 |
| --- | --- | --- |
| 落地结束后又播一次落地，最后卡在坐姿 | 物理每个 30 Hz tick 都向已着地的猫再施加重力，不断伪造 `.landed` | **已修复并有单元测试**：静止 120 tick 不再落地，真实下落只发出一次事件 |
| 高处轻放时落地播两次 | 松手瞬间先发送 `.landed`，真正撞地后物理层再次发送 | **已修复并有回归测试**：贴地轻放才直接落地；高处轻放保持空中状态，只有真实撞地发送一次落地，并解除抱起所有权 |
| 鼠标、漫游、系统状态同时重置身体动画 | 多个模块直接抢占动画，同级事件也从第 1 帧重播 | **已在代码修复**：`ActionCoordinator` 串行身体动作，同级进队列，相同动作只延长时间不重置帧 |
| 落地后立即被不兼容姿势接管 | 动作有队列，但还需要用 `startPose/endPose` 检查转场 | **已在代码与素材修复**：新增 `sitToStand/standToSit/lieDown/getUp` 四组 imagegen 24 帧过渡，并由姿势路由器插入；待实机完整链验收 |
| 系统一般状态频繁打断 | 工作区、音量、活跃度都被当作即时身体动作 | **已修复**：只有断网、低电量、锁屏、睡眠/唤醒可即时回应 |
| 工作、会议、音乐与闲置被读取但没有实际影响 | 系统事件在 `BehaviorEngine` 中直接丢弃，未传给自主选择器 | **已修复并有确定性测试**：工作偏向 `working`，会议只选安静行为，音乐偏向快乐/轻松行为，闲置偏向舔毛、等待和更早睡眠；只影响后续行为，不抢占当前动作 |

## 3. 鼠标意图、交互方向与右键

| 问题 | 根因 | 状态 |
| --- | --- | --- |
| 鼠标路过就触发追逐/抚摸，猫过分活跃 | 距离累计同时被多种交互使用 | **已在代码修复**：路过只看鼠标，300 ms 悬停才好奇，同一身体区域往返才抚摸，快速反转才驱散 |
| 鼠标在左侧，猫仍朝右 | 方向每采样重新判定，动作内可翻转 | **已修复代码路径**：识别手势时锁定方向到动作结束；待实机验收各身体区域 |
| 右键菜单时有时无 | 依赖全局监听先取消窗口穿透，存在时序竞争 | **构建 8 已实测基础路径**：先让光标进入当前猫轮廓再右键可稳定打开菜单；菜单期间手势暂停。权限拒绝路径仍待单独复核 |

## 4. 时序、自主行为和微动作

| 问题 | 根因 | 状态 |
| --- | --- | --- |
| 键鼠不动时猫也停止，长期一个站姿 | 行为时钟依赖显示回调，空闲/遮挡窗口会降频甚至停更 | **已修复并实测**：构建 8 连续运行超过 15 分钟，真实日志记录微动作、左右漫游、舔毛、翻肚、趴低、45 秒睡眠和自动醒来；连续 66 秒无指针事件期间自主链仍持续 |
| 微动作被注视或鼠标兴趣长期堵住 | 只有过于严格的 idle 条件才允许触发 | **已修复代码路径**：眼神层与身体层解耦，自主调度的到期动作等待串行执行；当前 `idle/idleEar/idleTail` 各 24 个不同帧，待 45% 实际尺寸确认耳尾幅度是否足够明显 |
| 动作整体过快 | 多数 24 帧在约 1 秒内播完 | **已按动作类型降速**到 8–18 FPS，并支持首尾非均匀停顿；已生成真实时长 GIF，待桌面观感复核 |
| 60/120 Hz 屏幕上位置移动仍像 30 Hz 阶梯 | 行为时钟、动画时钟和窗口物理共用 30 Hz watchdog | **已修复代码路径**：行为/素材动画保持独立 30 Hz 推进，窗口位置与抛掷物理改由显示回调按最高 120 Hz 积分；待真实 ProMotion 画面复核 |

## 5. 窗口、渲染、尺寸与裁切

| 问题 | 根因 | 状态 |
| --- | --- | --- |
| 黑色方框 | Metal/窗口透明通道配置不完整 | 源码已设置透明 panel、content layer 和 Metal view；待新包实机复测 |
| 猫太大且没有尺寸入口 | 窗口默认比例偏大、菜单入口不稳定 | 默认 45%，右键和爪印菜单提供 45/65/85% |
| 动作间整只猫变大/变小 | 旧素材按每个动作单独适配到固定格 | **运行路径已修复并通过自动 QA**：统一 `pixelsPerBodyUnit=220`，所有帧 `bodyScale=1.0`，不按单帧边界框缩放；待桌面转场复核 |
| 可变安全画布把猫整体托离桌面底部 | 物理地面错误地约束窗口底边，而不是共享脚底锚点 | **已修复并有回归测试**：透明安全区允许延伸到屏幕外，脚底世界坐标保持在桌面地面线上 |

## 6. 构建、旧进程和版本可见性

| 问题 | 根因 | 状态 |
| --- | --- | --- |
| 桌面出现 2–3 只猫 | 不同构建路径的旧实例并存 | **构建 8 已实测**：解锁运行时进程表只有 `/private/tmp/catatwork-release-8/猫上班了.app/.../CatAtWork` 一个实例 |
| 无法判断当前是旧代码还是新代码 | 菜单没有构建号、素材版本和当前动作 | 爪印菜单已增加应用版本/构建号、宠物包版本、当前动作/帧和日志入口 |

## 7. 诊断、测试与发布验收

- 已实现本机 JSONL 诊断日志：事件来源 → 粗粒度系统上下文 → 协调器决策 → 前后动作 → 队列长度 → 帧号。
- 日志不包含按键、窗口标题和精确鼠标轨迹，2 MB 轮换，不上传。
- 新增 `Scripts/summarize_interaction_log.py`，可自动归类重复落地、无自主/微动作、动作停滞、非移动姿势滑行和鼠标触发风暴，并输出最近 80 次动作的触发来源时间线。
- 默认宠物包当前包含 31 个正式动作，每个动作严格读取连续编号的 24 帧；旧横条或 `generated/` 草稿不能进入运行包。
- Swift 单元测试当前 36 项已通过，包含单次落地、高处轻放不提前落地、落地释放抱起状态、脚底世界锚点、上下文行为权重、串行忙碌窗口、鼠标意图分离、姿势转场以及 60 秒确定性自主行为。
- 31 动作预览、8→9/16→17/循环接缝检查及安全画布检查已完成；自动检查剩余两组自然大姿态警告为 `bellyPet` 和 `happy`，已人工检查联系表，仍须桌面动态复核。
- 构建 8 已完成 Universal 2 编译、ad-hoc Hardened Runtime 签名、DMG 系统校验和只读挂载验证；只将 `thrown` 空中旋转恢复至上一版 24 帧，`landing` 和运行逻辑保持构建 7 新版；内置包仍为 31×24 帧、16 注视方向。
- 尚未完成：3 分钟真实桌面录制、权限允许/拒绝双路径，以及正式 Developer ID/公证后的干净 Mac 验收。
